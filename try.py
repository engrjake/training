# Simple Moment Calculator

# input values
force = float(input("Enter force (N): "))
distance = float(input("Enter distance from pivot (m): "))

# calculation
moment = force * distance

# output result
print("Moment =", moment, "Nm")
